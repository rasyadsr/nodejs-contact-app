<h1>Hello, <%= nama %> 👋</h1>
<h4>Daftar Mahasiswa</h4>
<% if (mahasiswa.length < 1) { %>
<p>Data mahasiswa masih kosong</p>
<% } else { %> <% mahasiswa.forEach(value => { %>
<ul>
  <li><%= value.nama %></li>
  <li><%= value.email %></li>
</ul>
<% }) %> <% } %>
